Write a class of binary trees in arend. implement the function that makes mirror image of a given tree. prove that mirror (mirror t) = t for any binary tree t

in arend prove that the reverse function applied to a list is self-inverse. that is, reverse (reverse l) = l 
first, implement the function reverse itself

in arend implement the type of chain complexes over a given ring. 

